#include "dht.h"

const char *options(int num, int num1)
{
	if (num == 1)
		if (num = 'a')
			return ("Success");
		else
			return ("Failed");
	else if (num == 2)
		if (num = 'b')
			return ("Success");
		else
			return ("Failed");
	else if (num == 3)
		if (num = 'c')
			return ("Success");
		else
			return ("Failed");
	else if (num == 34)
		if (num = 'd')
			return ("Success");
		else
			return ("Failed");
	else
		return ("An error occured");
}
